# Python bytecode / caches
__pycache__/
*.py[cod]
*.pyd

# Test / typecheck caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments / local Python installs
.venv/
venv/
env/
.conda/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs
*.log

# Local secrets
.env
.env.*
!.env.example

# Cursor workspace data
.cursor/

# Local/temporary artifacts in repo root
nul

# Debug/runtime outputs (omit from VCS if team does not share traces/recorder data)
updates/recorder/
updates/traces/*.jsonl

# Debug/runtime outputs (omit from VCS if team does not share screenshots)
updates/screenshots/
