[pytest]
# 保证 `dom`、`models` 等包从项目根解析（勿用 `python tests/xxx.py` 直接跑单文件）
pythonpath = .
testpaths = tests
markers =
    ocr_window: 真实窗口 + Tesseract 的 OCR/DPI 集成（需 RUN_DESKTOP_INTEGRATION_TESTS=1、RUN_OCR_WINDOW_TESTS=1 与桌面会话）
